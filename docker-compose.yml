version: "3"

services:
  archiver:
    image: dougboutwell/redfeedbluefeed-archiver:latest
    volumes:
      - ./credentials:/home/archiver/credentials
      - ./archiver/config:/home/archiver/config

  site:
    image: dougboutwell/redfeedbluefeed-site:latest
    ports:
      - "80:80"
